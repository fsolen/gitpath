# gitpath
This project involves mounting the data from the Git repository near-sync in Linux paths on a hyperscale infrastructure with object storage. The system runs in delta-sync mode and uses jitter to avoid creating network storms.
